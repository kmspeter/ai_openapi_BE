# Copy this file to `.env` and fill in your provider API keys and runtime options.
# The FastAPI application loads these values using pydantic-settings.

# === Provider API keys ===
OPENAI_API_KEY=sk-proj-your-openai-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
GOOGLE_API_KEY=AIza-your-google-key

# === Database configuration ===
# Use the async SQLite driver by default. Replace with your own database URL if needed.
DATABASE_URL=sqlite+aiosqlite:///./data/usage.db

# === CORS settings ===
# Comma-separated list of origins allowed to access the API.
ALLOWED_ORIGINS=https://ai-modelhub-platform.vercel.app,http://localhost:5173

# === Application runtime ===
PORT=8000
DEBUG=True

# === Ngrok tunnel (optional) ===
# 외부 접근 허용하려면 True로 설정
ENABLE_NGROK=true

# ngrok 인증 토큰이 있다면 (https://dashboard.ngrok.com/get-started/your-authtoken)
NGROK_AUTHTOKEN=2gxxxxxxxxxxxxxxxxxxxxxxxxx

# 특정 리전을 사용할 경우 (us, eu, ap, au, in, jp, sa 중 선택)
NGROK_REGION=ap